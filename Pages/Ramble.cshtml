@page
@using Rambles.Data
@using Microsoft.AspNetCore.Components;
@inject RambleService rambleService

@{
    Layout = "_Layout";
    ViewData["Title"] = "Rambles";

    Ramble ramble = rambleService.GetRamble(Request.Path);
}

@Html.Raw(ramble.Text)